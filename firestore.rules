rules_version = '2';
// All analytics, lead and job data is written by Cloud Functions (Admin SDK bypasses rules).
// Browsers get no direct access.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

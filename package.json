{
  "name": "dentalassistantco",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build && npm --prefix functions run build",
    "deploy": "npm run build && firebase deploy",
    "deploy:hosting": "npm --prefix web run build && firebase deploy --only hosting",
    "wayback:fetch": "node scripts/fetch-wayback.mjs",
    "wayback:repair": "node scripts/repair-wayback.mjs"
  }
}

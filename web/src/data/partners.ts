// Organizations that may receive leads. This list is referenced by the TCPA consent text,
// so it MUST be updated before any lead is shared with a new partner.
export const partners: { name: string; type: "School" | "Employer" | "Staffing agency" }[] = [];
